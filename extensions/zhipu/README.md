# Zhipu Plugin

智谱 BigModel 的 web 搜索 provider，通过 MCP `web_search_prime` 端点返回带引用的
AI 综合结果。

配置 API Key：`plugins.entries.zhipu.config.webSearch.apiKey`，或设置
`ZHIPU_API_KEY` 环境变量。申请地址：<https://open.bigmodel.cn/>
